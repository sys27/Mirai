namespace Mirai.Emitting
{
    public enum StackBehaviourPush
    {
        Push0,
        Push1,
        PushI,
        PushRef,
        PushI8,
        PushR4,
        PushR8,
        Push1_Push1,
        VarPush,
    }
}